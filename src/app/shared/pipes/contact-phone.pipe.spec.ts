import { ContactPhonePipe } from './contact-phone.pipe';

describe('ContactPhonePipe', () => {
  it('create an instance', () => {
    const pipe = new ContactPhonePipe();
    expect(pipe).toBeTruthy();
  });
});
